# Caddy Runtime Data

This directory contains Caddy runtime data that is not being used in the current implementation. The project now uses Docker Compose for service orchestration.

## Purpose

This directory was intended to store:
- SSL/TLS certificates
- Caddy runtime configuration
- Log files

## Current Status

This directory is for reference only. The actual runtime data is now managed by Docker Compose.